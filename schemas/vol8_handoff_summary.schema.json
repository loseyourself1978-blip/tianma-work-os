{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/loseyourself1978-blip/tianma-work-os/schemas/vol8_handoff_summary.schema.json",
  "title": "Vol.8 Handoff Summary",
  "description": "Validates the Vol.8 Phase 8.6 static-only handoff summary fixture.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "volume",
    "phase",
    "baseline_commit",
    "runtime_records",
    "frameworks_indexed",
    "customer_facing_frameworks",
    "live_runtime_execution_frameworks",
    "fixture_static_read_only_frameworks",
    "validation_backed_frameworks",
    "static_shell_path",
    "static_shell_status",
    "vol8_status",
    "completed_phase_count",
    "completed_phases",
    "customer_facing_readiness",
    "network_allowed",
    "execution_allowed",
    "forbidden_scope_confirmed_absent",
    "handoff_status",
    "handoff_notes"
  ],
  "properties": {
    "volume": {"type": "string", "enum": ["Vol.8"]},
    "phase": {"type": "string", "enum": ["Vol.8 Phase 8.6 - Vol.8 Handoff Summary and Vol.9 Readiness Gate"]},
    "baseline_commit": {"type": "string", "enum": ["4836b20c373b40aa92a822ee09bb718fc953ae2d"]},
    "runtime_records": {"type": "integer", "enum": [113]},
    "frameworks_indexed": {"type": "integer", "enum": [24]},
    "customer_facing_frameworks": {"type": "integer", "enum": [0]},
    "live_runtime_execution_frameworks": {"type": "integer", "enum": [0]},
    "fixture_static_read_only_frameworks": {"type": "integer", "enum": [24]},
    "validation_backed_frameworks": {"type": "integer", "enum": [24]},
    "static_shell_path": {"type": "string", "enum": ["static_shell/ldd/"]},
    "static_shell_status": {"type": "string", "enum": ["local_static_read_only_fixture_only"]},
    "vol8_status": {"type": "string", "enum": ["completed"]},
    "completed_phase_count": {"type": "integer", "enum": [6]},
    "completed_phases": {"type": "array", "items": {"type": "string"}},
    "customer_facing_readiness": {"type": "boolean", "enum": [false]},
    "network_allowed": {"type": "boolean", "enum": [false]},
    "execution_allowed": {"type": "boolean", "enum": [false]},
    "forbidden_scope_confirmed_absent": {"type": "array", "items": {"type": "string"}},
    "handoff_status": {"type": "string", "enum": ["ready"]},
    "handoff_notes": {"type": "array", "items": {"type": "string"}}
  }
}
